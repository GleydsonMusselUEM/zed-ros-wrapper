# params/zed_yaml
# Parameters for Stereolabs ZED camera
---

general:
    camera_model:               'zed'
    grab_resolution:            'HD720'     # 'HD2K', 'HD1080', 'HD720', 'VGA', 'AUTO'
    grab_frame_rate:            27          # Frequency of frame grabbing for internal SDK operations

depth:
    min_depth:                  0.4         # Min: 0.3, Max: 3.0 - Default 0.7 - Note: reducing this value wil require more computational power and GPU memory
    max_depth:                  25        # Max: 40.0
